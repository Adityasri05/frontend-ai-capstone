import { Playground } from '../../../playground/Playground';

export const metadata = {
  title: 'Accessibility Playground | FlyRank AI Internship',
  description: 'Hand-built accessible React components: Modal, Tabs, Disclosure.',
};

export default function PlaygroundPage() {
  return <Playground />;
}
